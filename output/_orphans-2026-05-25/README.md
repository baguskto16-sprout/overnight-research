# Orphan pass-3 outputs (quarantined 2026-05-28)

These files were found loose at the repo root under `pass-3-deep-research/`
(should have been written inside a specific `output/raw-claude-overnight/<run-id>/`
directory). They have file mtimes of 2026-05-25 18:43 and look like outputs
from a logistics-topic re-run that misrouted its path. Filenames suggest the
`logistics-risk-mgmt-worldwide` topic (IATA/IUMI/Malacca/IDePOD/Nirvana).

Preserved here verbatim instead of deleted — content was unique (not
duplicated under any committed run dir). Two files (PP2.1-cost-1.json,
PP2.1-freq-1.json) share filenames with `2026-05-14-0107/pass-3-deep-research/`
but their content differs, so they are kept as-is.

Action item: review whether these belong to a specific run output or can be
discarded. Until then, this directory is the canonical location.
